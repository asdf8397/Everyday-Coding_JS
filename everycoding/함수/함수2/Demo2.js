/*
function numberings() {
    var algo = 0;
    while(algo < 20) {
        document.write(algo);
        algo += 1;
    }
}
*/

for(var algo = 0; algo<20; algo++) {
    var algo = 0;
    while(algo < 20) {
        document.write(algo+"<br/>");
    }
}